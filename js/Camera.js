define([], function () {

        var exported = {
            zoom:1,
            delta:{
                x:30,
                y:30
            }

        };

            return exported;
        }
    );