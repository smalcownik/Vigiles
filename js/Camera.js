define([], function () {

        var exported = {
            zoom:1,
            delta:{
                x:20,
                y:50
            }

        };

            return exported;
        }
    );